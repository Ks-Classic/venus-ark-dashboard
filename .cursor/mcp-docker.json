{
  "mcpServers": {
    "serena": {
      "command": "docker",
      "args": [
        "run",
        "--rm",
        "-i",
        "-v",
        "${workspaceFolder}:/app",
        "-w",
        "/app",
        "serena-mcp:latest",
        "--context",
        "ide-assistant",
        "--project",
        "."
      ]
    }
  }
}
